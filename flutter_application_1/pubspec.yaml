name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flame: ^1.34.0
  flame_tiled: ^3.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Add this section if it doesn't exist, or update it
  assets:
      # Based on your file explorer (for images)
    - assets/tiles/
    - assets/images/Terrain/   # <-- THIS LINE IS CRITICAL for level1.tmx 



